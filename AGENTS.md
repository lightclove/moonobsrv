# AGENTS.md — moonobsrv

Телеграм-бот наблюдений за небом на чистом Zig 0.15.2 (VOC, ретро-планеты, лунные дни). Сборка: `z.cmd build test` (тесты и астроякоря), `z.cmd build -Drelease` (ReleaseSmall). Тулчейн вложен в `tools/` — ничего ставить не нужно.

## Хронология moonobsrv (обязательно)

Любая новая возможность, правка поведения или ломающее изменение **в том же ходе** попадает в `docs/CHANGELOG.md`. Отдельной сессией «потом дописать» не оставлять.

- Дата — календарная `YYYY-MM-DD` из сегодняшней метки чата (не «сегодня», не «в этой сессии»).
- Версия — `x.y.z`. **MINOR** — новая возможность, **PATCH** — правка без новой возможности. **MAJOR** — только если человек явно сказал.
- Версия живёт одной строкой: `pub const version` в `src/runtime.zig` (её печатают `--version`, `/status`, `--help`).
- Незакрытое уже в коде — пункт в `[Unreleased]` с датой появления. Релиз: переименовать в `[x.y.z] — YYYY-MM-DD`, строка в таблицу **Хронология**, `runtime.version` = этот `x.y.z`.
- Запланированное, ещё не в коде — `docs/BACKLOG.md`. Взяли в ход — перенести в `[Unreleased]`, из backlog убрать.
- В каталоге у раздела — «с x.y.z / YYYY-MM-DD». Уточнение позже — вторая дата рядом, первую не стирать.
- Хеш коммита в истории — по желанию (git нет — дата обязательна).

```markdown
### [Unreleased] — 2026-09-09
#### Добавлено
- Коротко что и зачем.

### Холостая Луна — с 1.0.0 / 2026-09-09
```

## Учёт багов (обязательно)

Баг — любое неверное поведение, найденное где угодно (прод, тесты,
разработка, код-ревью). Учёт по карточкам, доска — `docs/BUGS.md`,
карточки — `docs/bugs/BUG-NNN-slug.md`, шаблон — `docs/bugs/_TEMPLATE.md`.

- Найден баг → карточка **в том же ходе**: файл + строка в таблицу своей
  колонки на доске (ОТКРЫТ). Номер — следующий свободный NNN.
- Починен → в карточке `Статус: ЗАКРЫТ`, поле «Фикс» (коммит/файл),
  заполнены «Лечение» и «Профилактика»; строка переезжает в колонку
  ЗАКРЫТ на доске; фикс — строкой в `docs/CHANGELOG.md` ([Unreleased] →
  Исправлено).
- Переоткрыли → статус снова ОТКРЫТ, в карточку — блок «Рецидив» с датой
  и что изменилось; прежний фикс не стирать.
- Статусов ровно два: ОТКРЫТ и ЗАКРЫТ. «Не баг, а просьба» — это
  `docs/BACKLOG.md`, не карточка.
- Доска обновляется вручную вместе с карточкой (сводка-счётчики тоже).
