# Backlog

Запланированное, ещё не в коде. Когда берём в ход — пункт переезжает в `docs/CHANGELOG.md` → `[Unreleased]`, отсюда убираем.

Незакрытое уже в коде, но без релиза — только в changelog, не сюда.

---

- Уровни подписок: «только станции Меркурия» без лунных дней, выбор событий при `/subscribe`.
- Место наблюдателя на подписчика (`/place 55.75 37.62` или геолокация Telegram) — сейчас восходы Луны считаются для одного места из `MOONOBSRV_LAT`/`LON`; уведомление о смене лунных суток «от восхода» (сейчас — только на границе титхи, с обоими номерами).
- Уведомления «за 30 минут до начала VOC» — упреждающий ватчер с lookahead-окном.
- Эфемеридные таблицы взамен рядов — минутная точность ингрессий/аспектов.
- Web App для наглядных графиков (фазы, VOC-календарь).
- systemd-юнит для хостового сторожа на Arch (`moonobsrv watch`) — образец запуска в README, установки нет.
- CLI: неизвестные флаги (`moonobsrv --unknown`) молча игнорируются и процесс уходит в сервисный режим — по-хорошему ругаться в stderr и выходить ненулевым кодом (зафиксировано при ручном прогоне B7 из MANUAL_TESTING.md, 2026-09-09).
